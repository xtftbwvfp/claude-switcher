{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default capability for Claude Switcher",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
